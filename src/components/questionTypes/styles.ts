import { CSSProperties } from 'react';

export const questionTypesStyles: Record<string, CSSProperties> = {
    'container': { maxWidth: 300 },
    'card': { margin: 10, display: 'flex', alignItems: 'center' },
    'cardText': { marginLeft: 20, marginTop: 0, marginBottom: 0 }
};
