import { CSSProperties } from 'react';

export const configStyles: Record<string, CSSProperties> = {
    'tabs': { marginTop: 10 },
    'page': { margin: 10, display: 'flex' }
}

export const appConfigStyles: Record<string, CSSProperties> = {};

export const formConfigStyles: Record<string, CSSProperties> = {};